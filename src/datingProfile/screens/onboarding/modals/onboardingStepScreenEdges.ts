/** Top inset is applied once on `ModalOnboardingFlow` (shared progress bar). */
export const ONBOARDING_STEP_SCREEN_EDGES = ['left', 'right'] as const;

export const ONBOARDING_STEP_SCREEN_EDGES_WITH_BOTTOM = ['bottom', 'left', 'right'] as const;
