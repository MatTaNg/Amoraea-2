/** Stubs — replace with expo-image-picker + Supabase storage integration. */
export async function selectPhotos(_max: number): Promise<string[]> {
  return [];
}

export async function removePhoto(_uri: string): Promise<void> {}
