/**
 * Re-export canonical marker slice aggregation + depth signals (single source of truth for app + edge).
 * @see supabase/functions/_shared/aggregateMarkerScoresFromSlices.ts
 */
export * from '../../../supabase/functions/_shared/aggregateMarkerScoresFromSlices';
