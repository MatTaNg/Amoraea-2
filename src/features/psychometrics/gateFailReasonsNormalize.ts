/**
 * Re-export canonical gate fail reason normalization (single source of truth for app + edge).
 * @see supabase/functions/_shared/gateFailReasonsNormalize.ts
 */
export * from '../../../supabase/functions/_shared/gateFailReasonsNormalize';
