/**
 * Onboarding / dating-profile assessment insight display tiers (UI copy triggers).
 */

export const ATTACHMENT_HIGH_ANXIETY_OR_AVOIDANCE_MIN = 4.0;

/** PVQ value dimension deviation from neutral for High/Low labels. */
export const PVQ_INSIGHT_HIGH_DEVIATION_MIN = 0.35;

/** DSIR overall score tiers. */
export const DSIR_INSIGHT_HIGH_MIN = 4.2;
export const DSIR_INSIGHT_LOW_MAX = 2.8;

/** Brief Resilience Scale insight tiers (1–6). */
export const BRS_INSIGHT_HIGH_MIN = 3.5;
export const BRS_INSIGHT_LOW_MAX = 2.5;
