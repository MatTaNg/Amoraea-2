/**
 * Contempt expression post-parse heuristic caps.
 */

/** Cap contempt_expression when engagement cues are absent (healthy-side scale). */
export const CONTEMPT_EXPRESSION_ABSENT_ENGAGEMENT_CAP = 6;
