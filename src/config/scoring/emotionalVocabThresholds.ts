/**
 * Personal-moment emotional vocabulary density thresholds.
 */

/** Below this absolute density (%) → vocab low. */
export const VOCAB_LOW_ABS_THRESHOLD = 0.3;

/** Below this ratio vs reference → vocab low. */
export const VOCAB_LOW_RATIO_TO_REFERENCE = 0.4;
